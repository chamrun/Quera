#include <stdio.h>
#include <string.h>

int main(){
      int firstX, secondX, firstWinner, secondWinner; 
      scanf("%d%d%d%d", &firstX, &secondX, &firstWinner, &secondWinner);
      if ((firstX >= firstWinner) && (secondX >= secondWinner)){
            printf("yes");
      } else{
            printf("no");
            }
      
      
      
      
      

}