#include <stdio.h>]

int numbOfNommi(int arr[], int lenght){
    int numb = 0;

    for (size_t i = 0; i < lenght; i++)    {
        scanf("%d", );
    }

}



int main( ) {
    int n;
    scanf("%d", &n);

    int a[n], b[n], sum = 0;

    for (size_t i = 0; i < n; i++)    {
        scanf("%d", &a[i]);
    }    

    for (size_t i = 0; i < n; i++)    {
        scanf("%d", &b[i]);
    }    

    for (size_t i = 0; i < n; i++)    {
        sum += a[i] * b[i];
        //printf("%d\n", sum);
    }

    printf("%d", sum);
    
    
}